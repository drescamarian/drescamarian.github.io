
const url = "https://jsonplaceholder.typicode.com/";

const users = "users/";

const posts = "posts/";

const comments = "comments";

const albums = "albums";

const photos = "photos";

const todos = "/todos";

const postsHome = "posts?_expand=user&_limit=5";

const ExpandUser = "?_expand=user";

const Page = "?_page=";

const Limit = "&_limit=" ;


export { url, users, posts, comments, albums, photos, todos, postsHome, ExpandUser, Limit, Page }